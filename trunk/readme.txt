Para generar el .jar ejecutar en el directorio TPE2011:
mvn clean package
De esa forma se generará el archivo simulation-1.0.one-jar.jar

Para configurar el host y el puerto del nodo y del servidor se puede usar un archivo .properties como config.properties, el cual será solicitado al iniciar la simulación.

